/** Shared unit for any time-based term: months or years. */
export type TermUnit = 'months' | 'years'

/** Shared currency type used across multiple calculators. */
export type Currency = 'AMD' | 'USD' | 'EUR'
